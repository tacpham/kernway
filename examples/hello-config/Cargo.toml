[package]
name    = "hello-config"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
kernway-config = { workspace = true }
kernway-log    = { workspace = true }

[lints]
workspace = true

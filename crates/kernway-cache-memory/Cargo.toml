[package]
name        = "kernway-cache-memory"
description = "Kernway in-memory cache — for testing and development, not production"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kernway-cache-core = { path = "../kernway-cache-core" }

[lints]
workspace = true

# docs.rs builds with every feature on, so feature-gated items still appear.
[package.metadata.docs.rs]
all-features = true

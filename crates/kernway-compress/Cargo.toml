[package]
name        = "kernway-compress"
description = "Shared HTTP compression — gzip/deflate/br encode + decode, content-type + Accept-Encoding negotiation; used by the server (compress) and the client (decompress)"
version.workspace    = true
edition.workspace    = true
license.workspace    = true

[dependencies]
# Pure-Rust codecs (no C): flate2 on miniz_oxide for gzip/deflate, brotli for br.
flate2 = { workspace = true }
brotli = { workspace = true }

[lints]
workspace = true
